@model IEnumerable<Bingit.Models.TXTLOPHOC>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MALOPHOC)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TENLOPHOC)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MALOPHOC)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TENLOPHOC)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MALOPHOC">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MALOPHOC">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MALOPHOC">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
